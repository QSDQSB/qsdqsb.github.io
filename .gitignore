_site/
.DS_Store
**/.DS_Store
node_modules/
docs
improvements.md
for_agents/
.playwright-cli
.sass-cache/
.claude/settings.local.json
.claude/worktrees/

# Photo pipeline (see _docs/photos-pipeline.md). Photographs live in R2, never
# in git: local originals go there with `npm run photos:push`, and the merged
# manifests are rebuilt from the bucket on every build. The old gallery/ tree
# and its generated thumbnails are no longer read by anything.
/gallery/
/images/thumbnails/gallery/
/_data/gallery_meta/
/photos/
/_data/photo_manifests/
/.photos-local/

.playwright-mcp
design/
# local secrets — loaded by scripts/send-letter.mjs, never committed
.env

# Python bytecode from importing a scripts/check-*.py module
__pycache__/

# Visual regression harness — only the baseline is tracked.
tests/visual/current/
tests/visual/diff/

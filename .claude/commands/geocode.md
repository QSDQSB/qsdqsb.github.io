Run `npm run geocode` to preprocess `_data/maps/*.yml` into `assets/maps/*.geojson`.

Before running, capture `ls -la assets/maps/` so you can report which cache files were created or updated.

After the run completes, list the changed files and any geocoding warnings the script printed.
